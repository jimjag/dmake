name: DMAKE-basic-check

on:
  push:
    branches: [ trunk ]
  pull_request:
    branches: [ trunk ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - run: ./configure
    - run: make
    - run: make check
    - run: make distclean
